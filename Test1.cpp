#include <bits/stdc++.h>

using namespace std ;

int main () {
    int b  = 3 , a = 0 ;
    a = b = 1 ;
    cout << a << " " << b << endl ;
    cout << pow(2,20) << endl ;
    for (int i = 1 , leng = (int) pow(2,i) ; i <= 20 ; i ++) {
    }
}
